
#include <iostream>
#include <stack>

using namespace std;

class sortedByStack{
public:
    void sorted(stack<int> s){
        stack<int> help;
        int cur;
        while(!s.empty()){
            cur = s.top();
            s.pop();
            while(!help.empty()&&)
        }
    }
};